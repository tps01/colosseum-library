# Copy to bench.toml next to the test script (USE_AUTOCONFIG is False for host).
# Optional thresholds for col.host.config / profile helpers; script limits use FILL_IN_HERE.

[host.profile]
profile_id = 1
min_disk_gb = 1
min_memory_mb = 256
