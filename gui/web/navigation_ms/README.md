# Navigation timing (web)

## FILL_IN_HERE

| Name | Meaning |
| --- | --- |
| `web_id` | `[[gui.web]]` id |
| `url` | Absolute URL to open |
| `max_ms` | Soft uppe... |

Requires `colosseum-shared` for `col.shared.verify.verify_field`, or replace
with a
dedicated GUI verifier when available.
